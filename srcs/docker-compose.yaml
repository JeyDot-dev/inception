name: inception
services:
  nginx:
    container_name:   nginx
    build:            requirements/nginx

    ports:
      - 80:80
      - 443:443

    restart:          always
    networks:
      - container_network

networks:
  container_network:
